# Google Directions API key (server-side only, never shipped in the app).
# Enable "Directions API" in Google Cloud Console for this key.
GOOGLE_MAPS_API_KEY=

# Optional: no longer used for routing, kept only if you re-enable Mapbox.
MAPBOX_TOKEN=
